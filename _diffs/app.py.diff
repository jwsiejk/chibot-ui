--- a/app.py+++ b/app.py@@ -558,6 +558,18 @@     resp = generate_response(user_text=text, history=aug_hist)
     reply = (resp.get("text") if isinstance(resp, dict) else str(resp or "")).strip()
     reply = _cap_words(reply, WORD_CAP)
+    # Guard against undesired static follow-up phrasing creeping into the reply
+    _BANNED_STATIC = {
+        "want me to go deeper, or keep it high level?",
+        "want me to go deeper on any part, or keep it high level?"
+    }
+    if reply and reply.strip().lower() in _BANNED_STATIC:
+        try:
+            reply = _clarify_from_state(ss, text)
+        except Exception:
+            # As a last resort, keep it empty to avoid repetition
+            reply = ""
+
 
     
 
@@ -871,8 +883,57 @@     except Exception:
         return False
 
+
 def _clarify_from_state(ss: "_SessionState", user_text: str) -> str:
+    """
+    Build a quick, varied clarifying question without any static catch‑all phrasing.
+    We randomize wording and personalize with any known product/task.
+    """
+    import random
     t = (user_text or "").strip()
+    prod = (ss.product or "").strip()
+    task = (ss.task or "").strip()
+
+    # Candidate templates (no "go deeper / high level" phrasing)
+    base = []
+    if prod and task:
+        base = [
+            f"For {prod} {task}, do you prefer a quick overview or step‑by‑step?",
+            f"On {prod} {task}, should I give a checklist or a concise summary?",
+            f"Working on {prod} {task} — do you want commands, a checklist, or a short overview?"
+        ]
+    elif prod:
+        base = [
+            f"On {prod}, are we installing, designing, or troubleshooting?",
+            f"For {prod}, would you like an overview or step‑by‑step?",
+            f"{prod} — should I outline key steps or just the essentials?"
+        ]
+    else:
+        # No state yet; tailor the ask using the user's text length as a hint
+        if len(t.split()) <= 3:
+            base = [
+                "Which product and version are you on, and what’s the goal?",
+                "What Pure product and version? And are you installing, designing, or troubleshooting?",
+                "Tell me product + version and the goal so I can get specific."
+            ]
+        else:
+            base = [
+                "Do you want a concise summary or step‑by‑step?",
+                "Should I give a quick overview or a punch‑list of steps?",
+                "Want a short summary or a checklist of actions?"
+            ]
+
+    # Shuffle to reduce repetition across turns
+    random.shuffle(base)
+    # Optionally remember the last line to avoid exact repeats; keep in-memory on session state if available
+    last = getattr(ss, "last_clarify", "")
+    for line in base:
+        if line != last:
+            ss.last_clarify = line
+            return line
+    # Fallback: return the first (still not the banned phrase)
+    ss.last_clarify = base[0]
+    return base[0]
     prod = (ss.product or "").strip()
     task = (ss.task or "").strip()
     if prod and task:
@@ -882,5 +943,5 @@     # fallbacks
     if len(t.split()) <= 3:
         return "Got it. Product and goal? I can tailor it fast."
-    return "Want me to go deeper, or keep it high level?"
-
+    return "Should I give a concise summary or step‑by‑step?"
+
