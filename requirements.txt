openai>=1.0.0
elevenlabs>=2.8.1
Flask>=2.2.0
Flask-Session>=0.4.0
authlib>=1.2.0              # for Google OAuth
itsdangerous>=2.1.2         # for magic link token signing
gunicorn                    # for production WSGI server
psycopg2-binary             # for PostgreSQL (Render)
python-dotenv               # for .env secrets
flask_cors
