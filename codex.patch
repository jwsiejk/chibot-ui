diff --git a/static/js/voice.js b/static/js/voice.js
index c2de95d..7c83918 100644
--- a/static/js/voice.js
+++ b/static/js/voice.js
@@ -17,6 +17,8 @@ Citations for context (non-functional):
     • CloseStream is emitted AFTER all audio chunks are queued to the socket (keep WS stream open while draining).
 */
 
+// Modularization notice: logic will progressively move into static/js/voice/* modules.
+
 import { VAD } from './voice/vad.js';
 import { sendAudioChunk, sendCloseStream, sendJSON, waitWSOpen } from './ws_module.js';
 import { stopPlayback, pausePlayback, resumePlayback, isPlaying as ttsIsPlaying } from './audio.js';
diff --git a/static/js/voice/core/Config.js b/static/js/voice/core/Config.js
new file mode 100644
index 0000000..f74fd8a
--- /dev/null
+++ b/static/js/voice/core/Config.js
@@ -0,0 +1,69 @@
+const DEFAULT_CONFIG = {
+  barge_in: {
+    mode: 'manual_only',
+  },
+  vad: {
+    enabled: true,
+    hysteresis: {
+      enter: 3,
+      exit: 4,
+    },
+  },
+  tts: {
+    mask_auto_vad: true,
+    decay_ms: 700,
+  },
+  evidence: {
+    snr_sigma: 2.5,
+    asr_conf: 0.65,
+  },
+  shadow: {
+    ms: 450,
+  },
+  commit: {
+    min_ms: 500,
+    drop_if_no_partial: true,
+  },
+  transport: {
+    close_on_turn_end: false,
+  },
+};
+
+export function getConfig() {
+  const source = (typeof window !== 'undefined' && window.__askchip_config) || {};
+
+  return {
+    barge_in: {
+      mode: source.barge_in?.mode ?? DEFAULT_CONFIG.barge_in.mode,
+    },
+    vad: {
+      enabled: source.vad?.enabled ?? DEFAULT_CONFIG.vad.enabled,
+      hysteresis: {
+        enter: source.vad?.hysteresis?.enter ?? DEFAULT_CONFIG.vad.hysteresis.enter,
+        exit: source.vad?.hysteresis?.exit ?? DEFAULT_CONFIG.vad.hysteresis.exit,
+      },
+    },
+    tts: {
+      mask_auto_vad: source.tts?.mask_auto_vad ?? DEFAULT_CONFIG.tts.mask_auto_vad,
+      decay_ms: source.tts?.decay_ms ?? DEFAULT_CONFIG.tts.decay_ms,
+    },
+    evidence: {
+      snr_sigma: source.evidence?.snr_sigma ?? DEFAULT_CONFIG.evidence.snr_sigma,
+      asr_conf: source.evidence?.asr_conf ?? DEFAULT_CONFIG.evidence.asr_conf,
+    },
+    shadow: {
+      ms: source.shadow?.ms ?? DEFAULT_CONFIG.shadow.ms,
+    },
+    commit: {
+      min_ms: source.commit?.min_ms ?? DEFAULT_CONFIG.commit.min_ms,
+      drop_if_no_partial:
+        source.commit?.drop_if_no_partial ?? DEFAULT_CONFIG.commit.drop_if_no_partial,
+    },
+    transport: {
+      close_on_turn_end:
+        source.transport?.close_on_turn_end ?? DEFAULT_CONFIG.transport.close_on_turn_end,
+    },
+  };
+}
+
+export { DEFAULT_CONFIG };
diff --git a/static/js/voice/core/index.js b/static/js/voice/core/index.js
new file mode 100644
index 0000000..84db637
--- /dev/null
+++ b/static/js/voice/core/index.js
@@ -0,0 +1 @@
+export { DEFAULT_CONFIG, getConfig } from './Config.js';
diff --git a/static/js/voice/index.js b/static/js/voice/index.js
new file mode 100644
index 0000000..55d54b8
--- /dev/null
+++ b/static/js/voice/index.js
@@ -0,0 +1,4 @@
+export * from './core/index.js';
+export * from './io/index.js';
+export * from './policy/index.js';
+export * from './ui/index.js';
diff --git a/static/js/voice/io/index.js b/static/js/voice/io/index.js
new file mode 100644
index 0000000..3d3d482
--- /dev/null
+++ b/static/js/voice/io/index.js
@@ -0,0 +1,2 @@
+// IO layer exports will be populated as modules migrate into this folder.
+export {};
diff --git a/static/js/voice/policy/index.js b/static/js/voice/policy/index.js
new file mode 100644
index 0000000..be98a52
--- /dev/null
+++ b/static/js/voice/policy/index.js
@@ -0,0 +1,2 @@
+// Policy layer exports will be populated as modules migrate into this folder.
+export {};
diff --git a/static/js/voice/ui/index.js b/static/js/voice/ui/index.js
new file mode 100644
index 0000000..875d91d
--- /dev/null
+++ b/static/js/voice/ui/index.js
@@ -0,0 +1,2 @@
+// UI layer exports will be populated as modules migrate into this folder.
+export {};
diff --git a/templates/index.html b/templates/index.html
index ff65f65..22c0ef8 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -107,6 +107,66 @@
   </div>
 
   <!-- Scripts -->
+  <script>
+    (function () {
+      const root = (window.__askchip_config = window.__askchip_config || {});
+      Object.assign(root, {
+        barge_in: root.barge_in || {},
+        vad: root.vad || {},
+        tts: root.tts || {},
+        evidence: root.evidence || {},
+        shadow: root.shadow || {},
+        commit: root.commit || {},
+        transport: root.transport || {},
+      });
+
+      if (typeof root.barge_in.mode === 'undefined') {
+        root.barge_in.mode = 'manual_only';
+      }
+
+      if (typeof root.vad.enabled === 'undefined') {
+        root.vad.enabled = true;
+      }
+      Object.assign(root.vad, {
+        hysteresis: root.vad.hysteresis || {},
+      });
+      if (typeof root.vad.hysteresis.enter === 'undefined') {
+        root.vad.hysteresis.enter = 3;
+      }
+      if (typeof root.vad.hysteresis.exit === 'undefined') {
+        root.vad.hysteresis.exit = 4;
+      }
+
+      if (typeof root.tts.mask_auto_vad === 'undefined') {
+        root.tts.mask_auto_vad = true;
+      }
+      if (typeof root.tts.decay_ms === 'undefined') {
+        root.tts.decay_ms = 700;
+      }
+
+      if (typeof root.evidence.snr_sigma === 'undefined') {
+        root.evidence.snr_sigma = 2.5;
+      }
+      if (typeof root.evidence.asr_conf === 'undefined') {
+        root.evidence.asr_conf = 0.65;
+      }
+
+      if (typeof root.shadow.ms === 'undefined') {
+        root.shadow.ms = 450;
+      }
+
+      if (typeof root.commit.min_ms === 'undefined') {
+        root.commit.min_ms = 500;
+      }
+      if (typeof root.commit.drop_if_no_partial === 'undefined') {
+        root.commit.drop_if_no_partial = true;
+      }
+
+      if (typeof root.transport.close_on_turn_end === 'undefined') {
+        root.transport.close_on_turn_end = false;
+      }
+    })();
+  </script>
   <script>
     (function () {
       const cfg = {{ askchip_config|tojson }};
