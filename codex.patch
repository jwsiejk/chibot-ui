diff --git a/app/security/jwt_utils.py b/app/security/jwt_utils.py
new file mode 100644
index 0000000000000000000000000000000000000000..5dfca41bef74f0f97d9cefbc2c808d9c98beff32
--- /dev/null
+++ b/app/security/jwt_utils.py
@@ -0,0 +1,39 @@
+"""JWT helper utilities for WebSocket authentication."""
+
+import logging
+import os
+import time
+import uuid
+
+import jwt
+
+_log = logging.getLogger("app.security.jwt")
+_SECRET = os.environ["SECRET_KEY"]
+_ALG = "HS256"
+_AUD = "chat.v2"
+_DEFAULT_TTL = 60
+
+
+def mint_ws_token(user_id: str, sid: str, is_admin: bool, ttl_s: int = _DEFAULT_TTL) -> str:
+    """Mint a short-lived WebSocket JWT for the given session."""
+    issued_at = int(time.time())
+    claims = {
+        "sub": user_id,
+        "sid": sid,
+        "aud": _AUD,
+        "iat": issued_at,
+        "exp": issued_at + ttl_s,
+        "jti": str(uuid.uuid4()),
+        "is_admin": is_admin,
+    }
+    _log.info("evt=ws_jwt_mint sid=%s ttl=%s", sid, ttl_s)
+    return jwt.encode(claims, _SECRET, algorithm=_ALG)
+
+
+def verify_ws_token(token: str) -> dict:
+    """Verify a WebSocket JWT and return its claims."""
+    try:
+        return jwt.decode(token, _SECRET, algorithms=[_ALG], audience=_AUD)
+    except jwt.PyJWTError as err:  # pragma: no cover - passthrough logging
+        _log.warning("evt=ws_jwt_verify_failed reason=%s", err.__class__.__name__)
+        raise
diff --git a/requirements.txt b/requirements.txt
index fd584bd97af0e1857e8fe5c7774d2ae61d4c054c..2963cc0fb653e1ee3abbe850377f98cf878d24a7 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -11,37 +11,41 @@ psycopg[binary]>=3.1.18     # pgcrypto/uuid-friendly; good on Render
 # --- HTTP clients (vendors & web calls) ---
 httpx>=0.27.0               # async+sync; use for OpenAI/ElevenLabs calls
 
 # --- Vendors ---
 openai>=1.30.0              # Chat Completions + Whisper
 elevenlabs>=1.6.1           # TTS + visemes client
 
 # --- CORS (off by default; enable via env/flag if UI/API split) ---
 Flask-Cors>=4.0.0
 
 # --- JSON / config utils ---
 orjson>=3.9.10              # fast JSON for logs/admin diagnostics payloads
 pydantic>=2.6.0             # request/response schemas (lightweight use)
 python-dotenv>=1.0.1        # local dev convenience; harmless on Render
 
 # --- Flask deps (pinned explicitly for stability) ---
 Jinja2>=3.1.3
 Werkzeug>=3.0.1
 itsdangerous>=2.1.2
 blinker>=1.7.0
 
 # --- Optional (safe to keep; remove if unused) ---
 python-multipart>=0.0.9     # if you accept uploads on ASGI routes (not needed for Flask uploads)
 
 
+# --- Security ---
+PyJWT>=2.8.0
+
+
 
 pytest
 
 requests
 
 alembic
 
 SQLAlchemy
 
 beautifulsoup4
 
 
